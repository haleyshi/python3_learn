'''
$ sudo apt-get install python3-pip
$ sudo pip3 install virtualenv 
$ virtualenv venv3
$ cd venv3
$ source bin/activate

$ pip install numpy
$ pip install pandas
$ pip install matplotlib
$ pip install seaborn

$ pip install sklearn

$ pip install jupyter
$ jupyter notebook
'''
